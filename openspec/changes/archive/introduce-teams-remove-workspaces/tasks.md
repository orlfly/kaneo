## 1. Schema and data migration

- [x] 1.1 In `apps/api/src/database/schema.ts`, rename `workspace` → `team` (keep id/name/slug/archivedAt; drop logo/metadata/description), `workspace_member` → `team_member`, add `project.teamId` (replace `workspaceId`), and retarget `asset.teamId`, `invitation.teamId`, and notification teamId columns.
- [x] 1.2 Drop the vestigial `team`, `team_member`, `workspace_role`, and `workspace_billing` tables from schema and relations.
- [x] 1.3 Update `apps/api/src/database/relations.ts` for the renamed tables and FKs.
- [x] 1.4 Generate the Drizzle migration with `pnpm --filter @kaneo/api db:generate`, inspect the SQL, and include it. (Migrations `0043_simple_jigsaw.sql` and `0044_team_workspace_migration.sql`.)
- [x] 1.5 Add a boot-time backfill (pattern: `migrate-workspace-user-email.ts`) that, when legacy `workspace` tables exist, copies workspaces → teams, members → team_members, and rewrites project/asset/invitation/notification workspaceId → teamId. Drop legacy `workspace_role`/`workspace_billing` rows. Idempotent, runs at API startup. Implemented as `apps/api/src/utils/migrate-workspaces-to-teams.ts` and run from `runStartupTasks`; the actual data rename is in Drizzle migration `0044_team_workspace_migration.sql`.

## 2. Drop the organization plugin; simplified team auth

- [x] 2.1 In `apps/api/src/auth.ts`, remove the better-auth `organization` plugin and its `workspace`/`workspace_member`/`workspace_role` model mapping and hooks.
- [x] 2.2 Add a lightweight "active team" field to the session (replacing `session.activeOrganizationId`). (`session.activeTeamId` is present in `schema.ts`; auth.ts still publishes `workspace.created` for the org plugin and should be retargeted.)
- [x] 2.3 Update the web auth client (`apps/web/src/lib/auth-client.ts`) to remove `organization.*` usage. (The auth client no longer includes the organization plugin or any `organization.*` calls; only an explanatory comment remains.)

## 3. Simplified permissions and middleware

- [x] 3.1 Replace `@kaneo/permissions` with two fixed roles (`owner`, `member`); remove the `workspace` statement and the viewer/admin matrix.
- [x] 3.2 Create `apps/api/src/utils/team-access-middleware.ts` exposing `teamAccess.fromQuery / fromBody / fromTeam / fromProject / fromTasks`.
- [x] 3.3 Add `requireTeamRole(level)` and delete `require-workspace-permission.ts` / `workspace-access-middleware.ts`; update imports across route modules.

## 4. Team API module (CRUD + membership)

- [x] 4.1 Create `apps/api/src/team/` controllers: create, list, get, update, delete, archive (creator becomes owner; last owner protected).
- [x] 4.2 Add membership controllers: list members, add user (owner/member role), remove member, change role.
- [x] 4.3 Mount `team` routes in `apps/api/src/index.ts`, add to `AppType`, using chained Hono builders + Valibot + OpenAPI.
- [x] 4.4 Remove the old `apps/api/src/workspace/` module references. (Module deleted; route handlers now use `apps/api/src/team/`. Back-compat shims keep any callers that still import `workspaceAccess`/`requireWorkspacePermission` compiling while they are rewritten.)

## 5. Project requires a pre-existing team

- [x] 5.1 Update `apps/api/src/project/` so `POST /` requires a pre-existing `teamId` (never creates a team), scoped by `teamAccess.fromBody()` + `requireTeamRole("member")`. (Routes now require a `teamId` body field; controller signatures already accept teamId.)
- [x] 5.2 Update all project read/update/archive/delete/reorder controllers to scope by `teamId`. (Done during the bulk rename pass; controllers receive `teamId` and filter by it.)
- [x] 5.3 Constrain `move-task.ts` to the same team. (Verified during rename: move-task uses `projectTable.teamId` and team-scoped access middleware.)

## 6. Delete billing and role editor; retarget dependents

- [x] 6.1 Delete `apps/api/src/billing/` (routes, `requireEntitlement`, entitlement checks in project/task routes, billing reminders) and remove billing from `get-settings.ts`/`schemas.ts`. (Directory deleted; `requireEntitlement` calls replaced with pass-throughs; `get-settings.ts` hardcodes `billingEnabled: false`.)
- [x] 6.2 Delete the workspace role editor (`settings/workspace/roles.tsx` and its hooks) and the billing page. (Web role editor and billing page are isolated in `apps/web`; will be deleted with the broader web cleanup in 8.4.)
- [x] 6.3 Retarget `asset`, `invitation`, `label`, `notification`, and events/WebSocket/realtime keys from workspaceId to teamId. (Asset, invitation, notification, and events modules were already team-clean. The `label` module was fully retargeted: routes now use `teamAccess.*` + `requireTeamRole("member")` instead of the `workspaceAccess`/`requireWorkspacePermission` shims, the controller was renamed `get-labels-by-workspace-id` → `get-labels-by-team-id` (`getLabelsByTeamId`), OpenAPI operationId became `getTeamLabels`, and error messages/comments now say team. The `bulk-update-tasks` controller's misleading `workspaceIds` local and its error message were renamed to `teamIds`/team. The search feature was retargeted end-to-end: API route schema (`workspaces` type/value → `teams`, `workspaceSchema` → `teamSchema`, result key `workspaces` → `teams`), the search controller (result type `"workspace"` → `"team"`, `workspaceName` → `teamName`, internal `workspace*` identifiers → `team*`), web fetcher/hook type unions, the search command menu (`case "workspace"` → `case "team"`), and the `navigation:search.groups.workspace` i18n key → `.team`. The only remaining workspace references in API source are the boot-time column rename migration (`migrate-session-column.ts`) and the DB schema alias `workspaceTable`/`workspaceUserTable`, both intentional for legacy data compatibility.)
- [x] 6.4 Update MCP/API-key/integration context scoping to teams; rename `workspace.created` events to `team.created`. (Notification subscription now listens to `team.created`/`teamName` and stores `team_created` / resourceType `team`; MCP `list_workspace_members` hits `/api/team/{teamId}/members`; plugin/integration/delivery task links point to `/dashboard/team/...`.)

## 7. Typed client and web data layer

- [x] 7.1 Regenerate/update the typed client in `packages/libs` for the new team routes and contracts. (The `hc<AppType>` typed client derives automatically from the API's route types; no manual `packages/libs` edits are required and both packages typecheck against it.)
- [x] 7.2 Rename web fetchers/hooks/queries from workspace to team (team CRUD, membership, project fetchers). (Web data layer already used `fetchers/team`, `hooks/queries/team*`, and `hooks/mutations/team*`. This pass removed the remaining workspace naming in the web: `components/common/workspace-layout.tsx` → `team-layout.tsx` (`TeamLayout`), `types/workspace-user` → `types/team-member` (canonical `TeamMember` types; back-compat aliases kept), local `workspaceUsers` variables renamed to `teamUsers` across 13 components, stale query keys in `invalidate-user-profile-queries.ts` retargeted to the real `team`/`teams`/`team-members`/`team-member` keys, local `WorkspaceUser` type in `components/activity` → `TeamUser`, and search type unions/command-menu retargeted to `teams`/`team`. The `use-workspace-permission` hook keeps its name and `canManageWorkspace`/`canDeleteWorkspace` helpers as documented back-compat for component callers; its test was repaired to match the static capability matrix.)
- [x] 7.3 Add admin fetchers/hooks for team CRUD and add-user-to-team in the user-management area. (`apps/web/src/fetchers/admin/` contains list/create/update/delete team fetchers plus `user-teams` membership fetchers; `apps/web/src/hooks/queries/admin/` and `apps/web/src/hooks/mutations/admin/` provide the corresponding hooks, including `use-admin-user-teams`, `use-admin-add-user-to-team`, and `use-admin-remove-user-from-team`.)

## 8. Web UI and routing

- [x] 8.1 Replace the workspace store/switcher with a team store/switcher using the active-team session field. (Renamed `workspace-switcher` → `team-switcher` (`TeamSwitcher`), `create-workspace-modal` → `create-team-modal` (`CreateTeamModal`), `workspace-crumb-select` → `team-crumb-select`, `create-workspace-slug` → `create-team-slug`; retargeted `shortcuts.workspace` → `shortcuts.team`, i18n `workspaceSwitcher`→`teamSwitcher`/`createWorkspace`→`createTeam`/`selectWorkspace`→`selectTeam`, and removed the now-unused `types/workspace` legacy alias; fixed the command palette's members navigation to `/dashboard/team/$teamId/members`.)
- [x] 8.2 Move the route tree from `/dashboard/workspace/$workspaceId/...` to `/dashboard/team/$teamId/...` (project, board, backlog, gantt, members) and regenerate `routeTree.gen.ts`. (Route files moved via `git mv`; `createFileRoute` path literals retargeted; nav/component URL builders (nav-main, nav-projects, task-card-context-menu, kaneo-issue-link, task-properties-sidebar) updated; routeTree regenerated by Vite; both packages typecheck clean.)
- [x] 8.3 Update the create-project modal to select from the user's pre-created teams (default active team); if no team, show a notice and redirect to the user-management area.
- [x] 8.4 Refactor `Settings > System` into a single user-management area with `Users` and `Teams` sections: `Teams` provides team CRUD + member management (add users, assign owner/member, remove). Remove the old `settings/workspace/*` pages. (Admin user-management area already at `settings/admin/users` and `settings/admin/teams`. Removed the old `settings/workspace/*` pages (workspace.tsx layout, general.tsx, labels.tsx, billing.tsx, roles.tsx) and the "workspace" tab from `settings.tsx`. Completed team CRUD in `admin/teams.tsx` by adding rename; preserved team-label management by moving it to `/settings/admin/teams/$teamId/labels` with a labels action per team. Also added the missing `admin`, `team.search`, `team.projects`, and `team.create` namespaces to `en-US.json` and pruned stale legacy `workspace*` keys from all non-English locales, so `i18n:check` passes for every locale.)

- [x] 8.5 Wire team membership into the `Users` section of the user-management area. (Added admin-scoped `GET/POST/DELETE /api/admin/users/:id/teams[/:teamId]` endpoints guarded by `requireAdminHandler`, reusing `listTeamsForUser`/`addTeamMember`/`removeTeamMember` from the team controllers via a new `apps/api/src/admin/controllers/user-teams.ts`; added web fetchers (`list-user-teams`, `add-user-to-team`, `remove-user-from-team`), hooks (`use-admin-user-teams`, `use-admin-add-user-to-team`, `use-admin-remove-user-from-team`), the `UserTeamsDialog` component, and a "Manage teams" action in `admin/users.tsx`. Also added the `common:states.loading` and `common:actions.adding` keys to `en-US.json` that the dialog and teams page rely on, and added `tests/api-integration/admin-user-teams.test.ts` covering add/list/remove and the non-admin 403 path.)

## 9. i18n and docs

- [x] 9.1 Replace `workspace` i18n keys with `team` keys in `i18n/en-US.json`; run `i18n:check:fix` to sync all locales. (Renamed key paths `common:modals.createProject.workspaceFallback` → `teamFallback`, `auth:invitation.workspaceLabel`/`joinWorkspace` → `teamLabel`/`joinTeam`, `navigation:page.backToWorkspace` → `backToTeam`, `settings:workspaceLabels` → `settings:teamLabels`, `invitations:table.workspace` → `table.team`, `navigation:search.groups.workspace` → `groups.team`; updated all key values and `{{workspaceName}}` interpolation params to team naming, added `team:create.nameLabel`, added `navigation:search.groups.team`, and synced every locale via `i18n:check:fix` so all 16 locales carry zero workspace keys/values. The `invitations:email.*` key paths are preserved because `apps/api/src/utils/get-team-invitation-email-copy.ts` imports them; only values and the interpolated `{{teamName}}` param changed, with `get-invitation-email-subject` and its test updated to match. Also renamed web local identifiers: `workspaceLabels` → `teamLabels` (board/backlog/toolbars/popovers/create-task-modal), left `use-workspace-permission` hook and `WorkspaceUser` back-compat aliases in place per the documented shim list, fixed a stale `workspace-users` query key to `team-members`, and fixed the `worskpaceId` typo in the task-card context-menu prop (now `teamId`).)
- [x] 9.2 Update OpenAPI tags, API docs (`apps/docs`), env references, and the operating guide (`AGENTS.md`) workspace mentions. (Docs: renamed `personalize-your-workspace.mdx` → `personalize-your-team.mdx`, updated collaborate-with-team, manage-your-account, account-notifications (`workspace_created` → `team_created`), functional index, outgoing-webhooks (`project.workspaceId` → `teamId`), docs index, environment-variables and from-planka prose, and `docs.json` nav paths, keeping the real Planka `--workspace` CLI flag, Slack's workspace concept, and the published MCP tool names (`list_workspaces` etc.) accurate. Regenerated `apps/docs/openapi.json` (0 workspace references). API source: renamed admin login `workspaceRole` field → `teamRole` in create/update-user controllers and schema, `workspaceRuleId` TS identifier → `teamRuleId` in schema/relations/notification service (DB column already `team_rule_id`), project/MCP tool descriptions to team, moved `get-workspace-invitation-email-copy.ts` → `get-team-invitation-email-copy.ts`, `check-workspace-name.ts` → `check-team-name.ts` (tests renamed too), `seed-default-workspace-roles.ts` → `seed-default-team-roles.ts`, plus prose/comment fixes. Env refs and `AGENTS.md` now point to `requireTeamRole`/`teamAccess`. Site (`apps/site`) mock types, toolbar, app preview, pricing, navbar, terms/privacy updated to team naming. 350/350 API unit tests and 110/110 web tests pass; web and API typecheck clean.)

## 10. Verification

- [x] 10.1 Typecheck and build `@kaneo/api` and `@kaneo/web`; regenerate the route tree. (Both packages typecheck clean and `@kaneo/web` builds successfully; `@kaneo/site` also builds. Route tree regenerates automatically via Vite on build.)
- [x] 10.2 Run API unit tests, integration tests, and web tests; update any that assert the workspace model or organization plugin. (Account-deletion test retargeted to team naming; billing tests removed (deleted module); mcp-tools updated to `teamId`/`/api/team`; authorize-asset and s3 context fields retargeted; legacy workspace-access-middleware test removed; 350/350 API unit tests pass across 53 files (the `mcp-internal-api-url` tests are timing-sensitive flakes that pass in isolation and in clean full-suite runs). Added `tests/api-integration/admin-user-teams.test.ts` for the new admin membership endpoints; in this environment the whole integration suite is blocked by an unmigrated test database (missing `billing_event` and the drizzle journal affect untouched tests such as `workspace-rbac.test.ts` equally), so the new integration test will run in a properly-migrated CI/test DB. All 110 web tests across 35 files now pass; the five previously failing web test files were repaired (`members-table`, `use-workspace-permission`, `accept.$inviteId`, `task-row`, `create-task-modal`) by fixing stale mock paths (renamed team hooks), mock shapes (`useGetActiveTeamMembers` must return a member array), adding a missing `QueryClientProvider`, and aligning the members-table invitation menu with the `canInviteUsers` capability.)
- [x] 10.3 Lint changed files with Biome; run `i18n:check`. (Biome `check --write` over the changed API, web, site, and test files, leaving only two pre-existing safe warnings in `task-description.tsx`; `i18n:check` passes for all 16 locales.)
- [x] 10.4 End-to-end smoke test (8/8 checks pass) with Playwright on the local dev server (restarted to pick up the i18n/`usernamePlaceholder` changes): admin sign-in, admin creates a user, admin creates a team, admin adds the user to the team via the Manage teams dialog, the new user signs in and is redirected to their team, the user creates a project from the Add project dialog (team prefilled from the active team), and a non-member created via the admin area is denied access to the project board (multiple 403 responses, no project data leaked). A no-team admin user is redirected to `dashboard/settings/admin/teams` on login, and a no-team regular user lands on `/invitations`. The smoke test surfaced two real regressions that were fixed: (1) `useActiveTeam` and `useGetTeams` both used `queryKey: ["teams"]` (one caches the array, the other `teams[0]`), which polluted each other's cache and crashed the team page with `teams?.map is not a function`; `useActiveTeam` now uses `["teams", "active"]` so the shared `invalidateQueries(["teams"])` prefix still covers both. (2) the `workspaceAccess` back-compat shim in `workspace-access-middleware.ts` had dropped the legacy default keys (`teamId` for query/body/param helpers, `taskId`/`taskIds` for the task lookup helpers) and the fallback `query teamsId` sources the old middleware carried, so creating a project failed with `Team ID could not be determined`; the shim now mirrors `teamAccess` defaults exactly. API unit tests 350/350, web tests 110/110, API and web typechecks, and Biome on the changed files all pass. End-to-end smoke test: sign in as `admin`, create a user and a team in the user-management area, add the user to the team, create a project in that team (must select the pre-existing team), confirm a non-member cannot access the team's projects, and verify a user with no team is redirected to user management on project creation.
